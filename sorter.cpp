# include "sorter.h"
#include "iconutil.h"

void Sorter::run(const Config& config) {
    // 1. get Namelist of Desktop Icons
    // 2. Sort them based on config.sorting
    // 3. Group them based on config.grouping

    std::string path = IconUtil::selectPath();
}

